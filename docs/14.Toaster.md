# Toaster
Toaster: A Toast is a little popup message that shows up on the screen.
- react-hot-toast

### install and import
```zsh
npm install react-hot-toast
```
```js
import toast from "react-hot-toast";
```
### -frontend/src/main.jsx
- <Toaster /> → sets up the “listener” for toast notifications.

```jsx
createRoot(document.getElementById("root")).render(
  <StrictMode>
    <BrowserRouter>
      <App />
      <Toaster />
    </BrowserRouter>
  </StrictMode>
);
```

### -frontend/src/App.jsx
- define app’s content & logic.
```jsx
const App = () => {
  return (
    <div>
      <button onClick={() => toast.success("congrates")}>click me</button>
      <Routes>
        <Route path="/" element={<HomePage />} />
        <Route path="/create" element={<CreatePage />} />
        <Route path="/note/:id" element={<NoteDetailPage />} />
      </Routes>
    </div>
  );
};
```